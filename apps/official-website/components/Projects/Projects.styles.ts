import { css } from 'glamor';
import type { IProjectsStyleProps } from './Projects.types';

export const Styles = ({}: IProjectsStyleProps) => {
  return { root: css({}) };
};

export default Styles;
