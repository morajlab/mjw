import type { IAvatarMenuStyleProps } from './AvatarMenu.types';

export const Styles = (_props?: IAvatarMenuStyleProps) => {
  return {
    menu: {
      itemLabel: {
        width: '100%',
      },
    },
  };
};

export default Styles;
