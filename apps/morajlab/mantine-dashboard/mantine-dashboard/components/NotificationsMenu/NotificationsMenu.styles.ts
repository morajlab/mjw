import type { INotificationsMenuStyleProps } from './NotificationsMenu.types';

export const Styles = (_props?: INotificationsMenuStyleProps) => {
  return {
    notification: {
      root: {
        boxShadow: 'none',
      },
    },
  };
};

export default Styles;
